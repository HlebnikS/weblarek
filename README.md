# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

#### Данные

В приложении используются две основные сущности данных: товар и покупатель.

Данные товара:

```ts
interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}
```

Расшифровка:
`interface IProduct` - интерфейс самого товара и что в него входит.
`id` - уникальный айди товара.
`description` - описание товара.
`image` - ссылка на изображение товара.
`title` - название товара.
`category` - категория товара.
`price` - цена товара. Значение `null` означает, что товар недоступен для покупки.

Данные покупателя:
Тип способа оплаты:

```ts
type TPayment = "card" | "cash";
```

Тип данных, описывающий доступные способы оплаты:

`card` — оплата картой.
`cash` — оплата при получении.

```ts
interface IBuyer {
  payment: TPayment;
  email: string;
  phone: string;
  address: string;
}
```

Расшифровка:
`interface IBuyer` - интерфейс покупателя.
`payment` - способ оплаты покупателя.
`email` - электронная почта покупателя.
`phone` - телефон покупателя.
`address` - адрес доставки.

#### Модели данных

Для работы с данными также используются три класса:
`ProductCatalog` — модель каталога товаров;
`Basket` — модель корзины;
`Buyer` — модель покупателя.

Класс `ProductCatalog`:
Класс отвечает за хранение и управление каталогом товаров.

Зона ответственности:

- хранит массив всех товаров;
- хранит товар, выбранный для подробного отображения;
- содержит методы:
  - сохранение массива товаров полученного в параметрах метода;
  - получение массива товаров из модели;
  - получение одного товара по его `id`;
  - сохранение товара для подробного отображения;
  - получение товара для подробного отображения.

Конструктор:
`constructor(events: IEvents)` - брокер событий, используемый для уведомления об изменении каталога и выбранного товара.

Поля класса:
`protected products: IProduct[]` - массив всех товаров.
`protected preview: IProduct | null` - товар, выбранный для подробного отображения.

Методы:
`setProducts(products: IProduct[]): void` - сохранение массива товаров полученного в параметрах метода.
`getProducts(): IProduct[]` - получение массива товаров из модели.
`getProduct(id: string): IProduct | undefined` - получение одного товара по его `id`.
`setPreview(product: IProduct): void` - сохранение товара для подробного отображения.
`getPreview(): IProduct | null` - получение товара для подробного отображения.

Класс `Basket`:
Класс отвечает за хранение товаров, выбранных пользователем для покупки.

Зона ответственности:

- хранит массив товаров, выбранных покупателем для покупки.
- методы:
  - получение массива товаров, которые находятся в корзине;
  - добавление товара, который был получен в параметре, в массив корзины;
  - удаление товара, полученного в параметре из массива корзины;
  - очистка корзины;
  - получение стоимости всех товаров в корзине;
  - получение количества товаров в корзине;
  - проверка наличия товара в корзине по его id, полученного в параметр метода.

Конструктор:
`constructor(events: IEvents)` — брокер событий, используемый для уведомления об изменении корзины.

Поля класса:
`protected items: IProduct[]` - массив товаров, выбранных покупателем для покупки.

Методы:
`getItems(): IProduct[]` - получение массива товаров, которые находятся в корзине.
`addItem(product: IProduct): void` - добавление товара, который был получен в параметре, в массив корзины.
`removeItem(product: IProduct): void` - удаление товара, полученного в параметре из массива корзины.
`clear(): void` - очистка корзины.
`getTotal(): number` - получение стоимости всех товаров в корзине.
`getCount(): number` - получение количества товаров в корзине.
`hasItem(id: string): boolean` - проверка наличия товара в корзине по его `id`, полученного в параметр метода.

Класс `Buyer`:
Класс отвечает за хранение, изменение и валидацию данных покупателя.

Зона ответственности:

- хранение данных покупателя:
  - вид оплаты;
  - адрес;
  - телефон;
  - email;
- методы:
  - сохранение данных в модели;
  - получение всех данных покупателя;
  - очистка данных покупателя;
  - валидация данных.

Конструктор:
`constructor(events: IEvents)` - брокер событий, используемый для уведомления об изменении данных покупателя.

Поля класса:
`protected payment: TPayment` - вид оплаты.
`protected address: string` - адрес доставки покупателя.
`protected phone: string` - телефон покупателя.
`protected email: string` - электронная почта покупателя.

Методы:
`setData(data: Partial<IBuyer>): void` - сохранение данных в модели.
`getData(): IBuyer` - получение всех данных покупателя.
`clear(): void` - очистка данных покупателя.
`validate(): Partial<Record<keyof IBuyer, string>>` - валидация данных.

#### Слой коммуникации

Для работы с сервером используется класс `WevLarekApi`.

Класс отвечает за получение данных сервера и отправку заказа на сервер.

Класс использует композицию: в конструктор передаётся объект `api`, который соответствует интерфейсу `IApi`.  
Для выполнения запросов используются методы `get` и `post` этого объекта.

Класс `WebLarekApi`:

Зона отвественности:

- получение списка товаров с сервера;
- отправка данных заказа на сервер.

Конструктор:
`constructor(api: IApi)` - принимает объект для работы с сервером.

Поля класса:
`protected api: IApi` - объект API, через который выполняются запросы.

Методы:
`getProducts(): Promise<IProductsResponse>` - выполняет GET-запрос на эндпоинт `/product/` и возвращает объект с массивом товаров.

`postOrder(order: IOrder): Promise<IOrderResult>` - выполняет POST-запрос на эндпоинт `/order/`, отправляет данные заказа и возвращает результат оформления заказа.

##### Документацию классов слоя Представления (View).

### Интерфейсы слоя Представления

Интерфейс `IHeaderData` - интерфейс описывает данные, необходимые для отображения шапки сайта.
Интерфейс использвуется классом `Header`.

```ts
interface IHeaderData {
  counter: number;
}
```

Поля интерфейса:
`counter` - количество товаров, добавленных в корзину.

Интерфейс `IGalleryData` - интерфейс описывает данные, необходимые для отображения каталога товаров.
Интерфейс используется классом `Gallery`.

```ts
interface IGalleryData {
  catalog: HTMLElement[];
}
```

Поля интерфейса:
`catalog` — массив готовых DOM-элементов карточек товаров.

Интерфейс `IModalData` - интерфейс описывает данные модального окна.
Интерфейсом используется классом `Modal`.

```ts
interface IModalData {
  content: HTMLElement;
}
```

Поля интерфейса:
`content`— DOM-элемент компонента, который необходимо отобразить внутри модального окна.

Интерфейс `ICardData` - интерфейс описывает данные, общие для всех вариантов карточки товара.
Используется абстрактным родительским классом `Card<T>`.

```ts
interface ICardData {
  id: string;
  title: string;
  price: number | null;
}
```

Поля интерфейса:
`id` — уникальный идентификатор товара;
`title` — название товара;
`price` — цена товара. Значение null означает, что товар недоступен для покупки.

Интерфейс `ICatalogCardData` - интерфейс описывает данные карточки товара в каталоге.
Интерфейс используется классом `CatalogCard`.

```ts
interface ICatalogCardData extends ICardData {
  category: string;
  image: string;
}
```

Поля интерфейса:
`category` — категория товара;
`image` — ссылка на изображение товара.

Также интерфейс наследует из `ICardData` следующие поля:

`id` — уникальный идентификатор товара;
`title` — название товара;
`price` — цена товара или `null`, если товар недоступен для покупки.

Интерфейс `IPreviewCardData` - интерфейс описывает данные подробной карточки товара.
Интерфейс используется классом `PreviewCard`.

```ts
interface IPreviewCardData extends ICatalogCardData {
  description: string;
  selected: boolean;
}
```

Поля интерфейса:
`description` — описание товара;
`selected` — признак нахождения товара в корзине.
Также интерфейс наследует из `ICatalogCardData` следующие поля:

`id` — уникальный идентификатор товара;
`title` — название товара;
`price` — цена товара или null, если товар недоступен для покупки;
`category` — категория товара;
`image` — ссылка на изображение товара.

Интерфейс `IBasketCardData` - интерфейс описывает данные карточки товара в корзине.
Интерфейс используется классом `BasketCard`.

```ts
interface IBasketCardData extends ICardData {
  index: number;
}
```

Поля интерфейса:
`index` — порядковый номер товара в списке корзины.

Также интерфейс наследует из `ICardData` следующие поля:

`id` — уникальный идентификатор товара;
`title` — название товара;
`price` — цена товара или null, если товар недоступен для покупки.

Интерфейс `IBasketViewData` - интерфейс описывает данные для отображения корзины.
Интерфейс используется классом `BasketView`.

```ts
interface IBasketViewData {
  items: HTMLElement[];
  total: number;
}
```

Поля интерфейса:
`items` — массив готовых DOM-элементов карточек товаров, добавленных в корзину;
`total` — итоговая стоимость товаров в корзине.

Интерфейс `IFormState` - интерфейс описывает общее состояние формы.
Используется абстрактным родительским классом `Form<T>`.

```ts
interface IFormState {
  valid: boolean;
  errors: string;
}
```

Поля интерфейса:
`valid` — признак корректного заполнения формы;
`errors` — сообщение об ошибке.

Интерфейс `IOrderFormData` - интерфейс описывает данные формы выбора способа оплаты и адреса доставки.
Интерфейс используется классом `OrderForm`.

```ts
interface IOrderFormData {
  payment: TPayment | null;
  address: string;
}
```

Поля интерфейса:
`payment` — выбранный способ оплаты. Значение `null` означает, что способ оплаты ещё не выбран;
`address` — адрес доставки.

Интерфейс `IContactsFormData` - интерфейс описывает данные формы контактной информации покупателя.
Интерфейс используется классом `ContactsForm`.

```ts
interface IContactsFormData {
  email: string;
  phone: string;
}
```

Поля интерфейса:
`email` — электронная почта покупателя;
`phone` — номер телефона покупателя.

Интерфейс `ISuccessData` - интерфейс описывает данные сообщения об успешном оформлении заказа.
Интерфейс используется классом `Success`.

```ts
interface ISuccessData {
  total: number;
}
```

Поля интерфейса:
`total` — итоговая сумма оформленного заказа.

Интерфейс `ICardActions` - интерфейс описывает действия, которые могут быть переданы компоненту карточки товара.
Используется классами: `CatalogCard`; `PreviewCard`; `BasketCard`.

```ts
interface ICardActions {
  onClick?: (event: MouseEvent) => void;
}
```

Поля интерфейса:

`onClick` — необязательный обработчик клика по карточке или её кнопке.

Интерфейс `IFormFieldChange<T>` - интерфейс описывает данные события изменения поля формы.

```ts
interface IFormFieldChange<T> {
  field: keyof T;
  value: string;
}
```

Поля интерфейса:
`field` — название изменённого поля;
`value` — новое значение поля.

Интерфейс `ISuccessActions` - интерфейс описывает действие, которое может быть передано компоненту успешного оформления заказа.
Используется классом `Success`.

```ts
interface ISuccessActions {
  onClick?: (event: MouseEvent) => void;
}
```

Поля интерфейса:

`onClick` — необязательный обработчик клика по кнопке возврата к покупкам.

###### Классы слоя Представления

Класс `Header` - класс отвечает за отображение шапки сайта и счётчика товаров в корзине.

Зона отвественности:
-отображение количества товаров в корзине;
-обработка клика по кнопке корзины;
-генерация события открытия корзины.

Конструктор:

```ts
constructor( protected events: IEvents, container: HTMLElement )
```

- принимает брокер событий;
- передаёт контейнер в родительский класс;
- находит счётчик `.header__basket-counter`;
- находит кнопку `.header__basket`;
- устанавливает обработчик клика по кнопке корзины.

Поля класса:

```ts
protected counterElement: HTMLElement; protected basketButton: HTMLButtonElement;
```

- counterElement — элемент, отображающий количество товаров;
- basketButton — кнопка открытия корзины;
- events — брокер событий, объявленный в параметрах конструктора.

Сеттеры:

```ts
set counter(value: number)
```

Обновляет число внутри элемента `.header__basket-counter`.

Класс `Gallery` - класс отвечает за отображение галереи карточек товаров.

Зона ответственности:

- получение готовых DOM-элементов карточек;
- отображение карточек в галерее;
- замена текущего содержимого галереи.

Конструктор:

```ts
constructor(container: HTMLElement)
```

Принимает DOM-элемент `.gallery` и передаёт его в родительский класс.

Поля класса:

```ts
protected catalogElement: HTMLElement;
```

Расшифровка:

`catalogElement` — контейнер галереи, в который помещаются карточки товаров.

Сеттеры:

```ts
set catalog(items: HTMLElement[])
```

Заменяет содержимое галереи переданным массивом DOM-элементов.

Класс `Modal` - класс универсального модального окна.

Зона ответственности:

- отображение переданного компонента;
- открытие модального окна;
- закрытие модального окна;
- очистка содержимого;
- обработка кнопки закрытия;
- обработка клика по оверлею.

Конструктор:

```ts
constructor(
  protected events: IEvents,
  container: HTMLElement
)
```

- передаёт контейнер в родительский класс;
- находит .modal\_\_content;
- находит кнопку .modal\_\_close;
- устанавливает обработчик кнопки закрытия;
- устанавливает обработчик клика по оверлею.

Поля класса:

```ts
protected contentElement: HTMLElement;
protected closeButton: HTMLButtonElement;
```

Расшифровка:

- contentElement — контейнер содержимого модального окна;
- closeButton — кнопка закрытия модального окна;
- events — брокер событий.

Сеттеры:

```ts
set content(value: HTMLElement)
```

Заменяет содержимое .modal\_\_content переданным DOM-элементом.

Методы:

```ts
open(): void
```

Открывает модальное окно.

```ts
close(): void
```

Закрывает модальное окно и очищает его содержимое.

Класс `Card<T>` - абстрактный родительский класс для трёх вариантов карточки товара.
От класса наследуются:

`CatalogCard`;
`PreviewCard`;
`BasketCard`.

Зона ответственности:

- запись идентификатора товара в DOM-элемент карточки;
- отображение названия;
- отображение цены;
- обработка случая, когда цена равна null.

Конструктор:

```ts
constructor(container: HTMLElement)
```

Передаёт корневой DOM-элемент карточки в класс Component<T> и находит общие элементы карточки.

Поля класса:

```ts
protected titleElement: HTMLElement;
protected priceElement: HTMLElement;
```

Расшифровка:

`titleElement` — элемент `.card__title`;
`priceElement` — элемент `.card__price`.

Сеттеры:

```ts
set id(value: string)
set title(value: string)
set price(value: number | null)
```

- записывает идентификатор товара в атрибут `data-id` корневого DOM-элемента карточки.
- Устанавливает название товара.
- Устанавливает цену товара.

Класс `Card<T>` не обрабатывает конкретные действия пользователя. Обработчики передаются дочерним классам через объект `ICardActions`.

Класс `CatalogCard` - класс отвечает за отображение карточки товара в каталоге.

Зона ответственности:

- отображение категории товара;
- отображение изображения;
- отображение общих данных карточки;
- обработка клика по карточке через переданное действие.

Конструктор:

```ts
constructor(
  container: HTMLElement,
  actions?: ICardActions
)
```

- передаёт контейнер в родительский класс Card;
- находит .card\_\_category;
- находит .card\_\_image;
- при наличии actions.onClick назначает обработчик клика на корневой элемент карточки.

Поля класса:

```ts
protected imageElement: HTMLImageElement;
protected categoryElement: HTMLElement;
```

`imageElement` — изображение товара;
`categoryElement` — элемент категории товара.

Сеттеры:

```ts
set category(value: string)
set image(value: string)
```

Устанавливает текст категории и соответствующий CSS-модификатор.
Устанавливает изображение через унаследованный метод `setImage()`.

Класс `PreviewCard` - класс отвечает за подробное отображение выбранного товара.

Зона ответственности:

- отображение изображения товара;
- отображение категории;
- отображение описания;
- отображение общих данных карточки;
- управление кнопкой покупки;
- вызов переданного обработчика действия.

Конструктор:

```ts
constructor(
  container: HTMLElement,
  actions?: ICardActions
)
```

- передаёт контейнер в родительский класс;
- находит `.card__image`;
- находит `.card__category`;
- находит `.card__text`;
- находит кнопку `.card__button`;
- при наличии `actions.onClick` назначает обработчик на кнопку.

Поля класса:

```ts
protected imageElement: HTMLImageElement;
protected categoryElement: HTMLElement;
protected descriptionElement: HTMLElement;
protected actionButton: HTMLButtonElement;
```

`imageElement` — изображение товара;
`ategoryElement` — категория товара;
`descriptionElement` — описание товара;
`actionButton` — кнопка действия.

Сеттеры:

```ts
set image(value: string)
set category(value: string)
set description(value: string)
set selected(value: boolean)
set price(value: number | null)
```

- Устанавливает изображение товара.
- Устанавливает категорию и CSS-модификатор.
- Устанавливает описание товара.
- Изменяет состояние кнопки в зависимости от наличия товара в корзине.
- Устанавливает цену и управляет доступностью кнопки.

Класс `BasketCard` - класс отвечает за отображение одного товара в корзине.

Зона ответственности:

- отображение порядкового номера товара;
- отображение названия;
- отображение цены;
- обработка кнопки удаления через переданное действие.

Конструктор:

```ts
constructor(
  container: HTMLElement,
  actions?: ICardActions
)
```

- передаёт контейнер в родительский класс;
- находит `.basket__item-index`;
- находит `.basket__item-delete`;
- при наличии `actions.onClick` назначает обработчик на кнопку удаления.

Поля класса:

```ts
protected indexElement: HTMLElement;
protected deleteButton: HTMLButtonElement;
```

`indexElement` — порядковый номер товара;
`deleteButton` — кнопка удаления товара.

Сеттеры:

```ts
set index(value: number)
```

Устанавливает порядковый номер товара в корзине.

Класс `BasketView` - класс отвечает за отображение содержимого корзины.

Зона ответственности:

- отображение списка товаров;
- отображение итоговой стоимости;
- управление кнопкой оформления заказа;
- обработка перехода к оформлению.

Конструктор:

```ts
constructor(
  protected events: IEvents,
  container: HTMLElement
)
```

- передаёт контейнер в родительский класс;
- находит .basket\_\_list;
- находит .basket\_\_price;
- находит .basket\_\_button;
- устанавливает обработчик клика по кнопке оформления.

Поля класса:

```ts
protected listElement: HTMLElement;
protected totalElement: HTMLElement;
protected orderButton: HTMLButtonElement;
```

`listElement` — список товаров корзины;
`totalElement` — итоговая стоимость;
`orderButton` — кнопка оформления заказа.

Сеттеры:

```ts
set items(items: HTMLElement[])
set total(value: number)
```

- Заменяет содержимое списка переданными карточками товаров.
- Отображает итоговую стоимость товаров.

Класс `Form<T>` - абстрактный родительский класс для форм оформления заказа.

Зона ответственности:

- обработка изменения полей;
- обработка отправки формы;
- генерация событий;
- управление доступностью кнопки;
- отображение ошибок;
- обновление состояния формы.

Конструктор:

```ts
constructor(
  protected events: IEvents,
  container: HTMLFormElement
)
```

- передаёт форму в родительский класс;
- находит кнопку отправки;
- находит .form\_\_errors;
- устанавливает обработчик input;
- устанавливает обработчик submit.

Поля класса:

```ts
protected submitButton: HTMLButtonElement;
protected errorsElement: HTMLElement;
```

`submitButton` — кнопка отправки формы;
`errorsElement` — элемент сообщения об ошибке.

Сеттеры:

```ts
set valid(value: boolean)
set errors(value: string)
```

- Управляет доступностью кнопки отправки.
- Отображает сообщение об ошибке.

Методы:

```ts
protected onInputChange(
  field: keyof T,
  value: string
): void
```

- Генерирует событие изменения поля.

Класс `OrderForm` - класс отвечает за форму выбора способа оплаты и адреса доставки.

Зона ответственности:

- отображение способов оплаты;
- отображение выбранного способа оплаты;
- отображение адреса;
- обработка кнопок выбора оплаты.

Конструктор:

```ts
constructor(
  events: IEvents,
  container: HTMLFormElement
)
```

- вызывает конструктор родительского класса;
- находит кнопки `button[name="card"]` и `button[name="cash"]`;
- находит поле `input[name="address"]`;
- устанавливает обработчики кнопок оплаты.

Поля класса:

```ts
protected paymentButtons: HTMLButtonElement[];
protected addressInput: HTMLInputElement;
```

`paymentButtons` — кнопки выбора способа оплаты;
`addressInput` — поле адреса доставки.

Сеттеры:

```ts
set payment(value: TPayment | null)
set address(value: string)
```

- Изменяет визуальное состояние выбранной кнопки оплаты.
- Устанавливает значение адреса.

Класс `ContactsForm` - класс отвечает за форму контактных данных покупателя.

Зона ответственности:

- отображение email;
- отображение телефона;
- отображение ошибок;
- управление кнопкой оплаты.

Конструктор:

```ts
constructor(
  events: IEvents,
  container: HTMLFormElement
)
```

- вызывает конструктор родительского класса;
- находит поле `input[name="email"]`;
- находит поле `input[name="phone"]`.

Поля класса:

```ts
protected emailInput: HTMLInputElement;
protected phoneInput: HTMLInputElement;
```

`emailInput` — поле электронной почты;
`phoneInput` — поле телефона.

Сеттеры:

```ts
set email(value: string)
set phone(value: string)
```

- Устанавливает значение электронной почты.
- Устанавливает значение телефона.

Класс `Success` - класс отвечает за отображение сообщения об успешном оформлении заказа.

Зона ответственности:

- отображение сообщения об успешном заказе;
- отображение списанной суммы;
- обработка кнопки возврата к покупкам.

Конструктор:

```ts
constructor(
  container: HTMLElement,
  actions?: ISuccessActions
)
```

- передаёт контейнер в родительский класс;
- находит `.order-success__description`;
- находит `.order-success__close`;
- при наличии обработчика назначает его кнопке закрытия.

Поля класса:

```ts
protected descriptionElement: HTMLElement;
protected closeButton: HTMLButtonElement;
```

`descriptionElement` — элемент итоговой суммы;
`closeButton` — кнопка возврата к покупкам.

Сеттеры:

```ts
set total(value: number)
```

- Отображает итоговую сумму заказа.

### События приложения

Событие `basket:open` - генерируется классом `Header` при нажатии пользователя на кнопку корзины.

Событие сообщает Presenter о необходимости отобразить содержимое корзины.

Данные с событием не передаются.

Событие `modal:close` - генерируется классом `Modal`:

при нажатии на кнопку закрытия;
при клике по области оверлея.

Событие сообщает Presenter о необходимости закрыть модальное окно.

Данные с событием не передаются.

Событие `order:open` - генерируется классом `BasketView` при нажатии на кнопку оформления заказа.

Событие сообщает Presenter о необходимости отобразить форму выбора способа оплаты и адреса доставки.

Данные с событием не передаются.

Событие `order.payment:change` - генерируется классом `OrderForm` при выборе пользователем способа оплаты.

Данные события:

```ts
{
  field: "payment";
  value: "card" | "cash";
}
```

`field` — название изменённого поля;
`value` — выбранный способ оплаты.

Событие `order.address:change`

Генерируется родительским классом `Form<T>` при изменении поля адреса в форме `OrderForm`.

Данные события:

```ts
{
  field: "address";
  value: string;
}
```

`field` — название изменённого поля;
`value` — введённый адрес доставки.

Событие `order:submit` - генерируется родительским классом `Form<T>` при отправке формы `OrderForm`.

Событие сообщает Presenter о переходе к форме контактных данных.

Данные с событием не передаются.

Событие `contacts.email:change` - генерируется родительским классом `Form<T>` при изменении электронной почты в форме `ContactsForm`.

Данные события:

```ts
{
  field: "email";
  value: string;
}
```

`field` — название изменённого поля;
`value` — введённая электронная почта.

Событие `contacts.phone:change` - генерируется родительским классом `Form<T>` при изменении телефона в форме `ContactsForm`.

Данные события:

```ts
{
  field: "phone";
  value: string;
}
```

`field` — название изменённого поля;
`value` — введённый номер телефона.

Событие `contacts:submit` - генерируется родительским классом `Form<T>` при отправке формы `ContactsForm`.

Событие сообщает Presenter о необходимости проверить данные и отправить заказ на сервер.

Данные с событием не передаются.

### События моделей данных

Модели данных используют брокер событий `IEvents` для уведомления Presenter об изменении своего состояния.

Один экземпляр брокера событий передаётся во все модели через конструктор:

```ts
const events = new EventEmitter();

const catalog = new ProductCatalog(events);
const basket = new Basket(events);
const buyer = new Buyer(events);
```

События генерируются только после изменения данных. Методы, которые только возвращают или проверяют данные, события не генерируют.

Событие `catalog:changed`

Генерируется моделью `ProductCatalog` после сохранения нового массива товаров методом:

```ts
setProducts(products: IProduct[]): void
```

Событие сообщает Presenter, что данные каталога изменились и карточки товаров необходимо отобразить заново.

Передаваемые данные:

```ts
{
  products: IProduct[];
}
```

`products` — актуальный массив товаров каталога.

Событие `preview:changed`

Генерируется моделью `ProductCatalog` после изменения выбранного товара методом:

```ts
setPreview(product: IProduct): void
```

Событие сообщает Presenter, что необходимо отобразить подробную карточку выбранного товара.

Передаваемые данные:

```ts
{
  product: IProduct;
}
```

`product` — выбранный товар.

Событие `basket:changed`

Генерируется моделью `Basket` после любого изменения содержимого корзины:

добавления товара методом addItem();
удаления товара методом removeItem();
очистки корзины методом clear().

Передаваемые данные:

```ts
{
  items: IProduct[];
  total: number;
  count: number;
}
```

`items` — актуальный массив товаров в корзине;
`total` — итоговая стоимость товаров;
`count` — количество товаров в корзине.

Событие сообщает Presenter о необходимости обновить:

содержимое корзины;
итоговую стоимость;
счётчик товаров в шапке сайта.

Событие `buyer:changed`

Генерируется моделью Buyer после изменения данных покупателя методом:

```ts
setData(data: Partial<IBuyer>): void
```

Также событие генерируется после очистки данных методом:

```ts
clear(): void
```

Передаваемые данные:

```ts
{
  data: IBuyer;
}
```

`data` — актуальные данные покупателя.

Событие сообщает Presenter о необходимости выполнить валидацию данных и обновить состояние соответствующей формы.

### Презентер

Презентер отвечает за взаимодействие между Моделями данных и классами Представления.

Поскольку приложение WebLarek состоит из одной страницы, Презентер реализован в основном файле приложения:

`src/main.ts`

Отдельный класс для Презентера не создаётся.

- создаёт экземпляры Моделей данных;
- создаёт экземпляры классов Представления;
- подписывается на события приложения;
- получает актуальные данные из Моделей;
- передаёт данные классам Представления;
- вызывает методы Моделей для изменения данных;
- управляет последовательностью открытия модальных окон;
- формирует данные заказа;
- вызывает методы API.

Презентер не хранит данные приложения. Все данные хранятся в Моделях:

`ProductCatalog`;
`Basket`;
`Buyer`.

Презентер не изменяет DOM напрямую. Для изменения интерфейса используются классы Представления.

Обновление Представления выполняется:

- при обработке события Модели данных, сообщающего об изменении данных;
- при обработке действия открытия модального окна.

Презентер не должен генерировать новые события внутри обработчиков событий. Он обрабатывает полученные события и вызывает соответствующие методы Моделей и Представлений.

Обмен информацией между частями приложения выполняется через единый экземпляр брокера событий:
`const events = new EventEmitter()`;

Один экземпляр `events` передаётся всем Моделям и компонентам Представления.

https://github.com/HlebnikS/weblarek
